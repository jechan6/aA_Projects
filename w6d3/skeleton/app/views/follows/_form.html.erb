
  <% followed = current_user.follows?(user) %>
  <button type="button" class="follow-toggle" data-user-id="<%=user.id%>" 
    data-initial-follow-state="<%=followed ? 'followed' : 'unfollowed'%>" name="button"></button>

